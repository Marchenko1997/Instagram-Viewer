export const selectHighlights = (state) => state.highlights.highlights || [];
export const selectIsLoading = (state) => state.highlights.isLoading;
export const selectError = (state) => state.highlights.error;
